# =============================================================================
# AWS Credentials for External Secrets Operator - TEMPLATE
# =============================================================================
# IMPORTANT: This is a TEMPLATE file. The actual secret is created by the
# bootstrap script (bootstrap.sh) during cluster initialization.
#
# This file documents the expected secret structure. DO NOT commit actual
# credentials to this file - GitHub will block the push.
#
# The bootstrap script creates this secret with:
#   kubectl create secret generic aws-external-secrets-creds \
#     --from-literal=access-key-id="$ACCESS_KEY_ID" \
#     --from-literal=secret-access-key="$SECRET_ACCESS_KEY" \
#     --namespace=external-secrets
#
# Required IAM permissions for the AWS user:
# - secretsmanager:GetSecretValue
# - secretsmanager:ListSecrets
# - ecr:GetAuthorizationToken
# =============================================================================
# apiVersion: v1
# kind: Secret
# metadata:
#   name: aws-external-secrets-creds
#   namespace: external-secrets
#   labels:
#     app.kubernetes.io/name: external-secrets
#     app.kubernetes.io/component: aws-credentials
# type: Opaque
# stringData:
#   access-key-id: "<AWS_ACCESS_KEY_ID>"
#   secret-access-key: "<AWS_SECRET_ACCESS_KEY>"
