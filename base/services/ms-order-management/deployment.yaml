# =============================================================================
# ms-order-management - Order Management Service
# =============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-order-management
  labels:
    app: ms-order-management
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-order-management
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: ms-order-management
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3001"
        prometheus.io/path: "/metrics"
    spec:
      imagePullSecrets:
        - name: ecr-cred
      containers:
        - name: ms-order-management
          image: 478468757808.dkr.ecr.ap-south-1.amazonaws.com/kubestock/ms-order-management:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3005
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3005"
            - name: HOST
              value: "0.0.0.0"
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: DB_HOST
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: "order_db"
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: DB_PASSWORD
            - name: INVENTORY_SERVICE_URL
              value: http://ms-inventory:3003
            - name: PRODUCT_SERVICE_URL
              value: http://ms-product:3002
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          readinessProbe:
            httpGet:
              path: /health
              port: 3005
            initialDelaySeconds: 30
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 3005
            initialDelaySeconds: 40
            periodSeconds: 10
