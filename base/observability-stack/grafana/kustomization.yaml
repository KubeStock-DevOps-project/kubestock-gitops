# =============================================================================
# Grafana Component - Kustomization
# =============================================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - dashboard-provisioning.yaml
  - dashboard-kubernetes.yaml
  - dashboard-microservices.yaml
  - dashboard-logs.yaml
  - dashboard-test-runner.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
