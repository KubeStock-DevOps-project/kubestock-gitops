# =============================================================================
# Staging Secrets
# =============================================================================
# NOTE: In production, use sealed-secrets or external-secrets operator
# These are placeholder values - MUST be updated before deployment
# =============================================================================
---
apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: kubestock-staging
type: Opaque
stringData:
  # RDS PostgreSQL Connection
  DB_HOST: "CHANGE_ME.ap-south-1.rds.amazonaws.com"
  DB_USER: "kubestock"
  DB_PASSWORD: "CHANGE_ME_staging_password_2024"
---
apiVersion: v1
kind: Secret
metadata:
  name: asgardeo-secret
  namespace: kubestock-staging
type: Opaque
stringData:
  # Asgardeo Organization
  ASGARDEO_ORG_NAME: "kubestock"
  ASGARDEO_BASE_URL: "https://api.asgardeo.io/t/kubestock"
  # M2M Application Credentials - CHANGE THESE
  ASGARDEO_M2M_CLIENT_ID: "CHANGE_ME"
  ASGARDEO_M2M_CLIENT_SECRET: "CHANGE_ME"
  # OAuth/OIDC Endpoints
  ASGARDEO_TOKEN_URL: "https://api.asgardeo.io/t/kubestock/oauth2/token"
  ASGARDEO_SCIM2_URL: "https://api.asgardeo.io/t/kubestock/scim2"
  ASGARDEO_JWKS_URL: "https://api.asgardeo.io/t/kubestock/oauth2/jwks"
  ASGARDEO_ISSUER: "https://api.asgardeo.io/t/kubestock/oauth2/token"
  # Group IDs - CHANGE THESE
  ASGARDEO_GROUP_ID_ADMIN: "CHANGE_ME"
  ASGARDEO_GROUP_ID_SUPPLIER: "CHANGE_ME"
  ASGARDEO_GROUP_ID_WAREHOUSE_STAFF: "CHANGE_ME"
